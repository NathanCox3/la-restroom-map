API_PORT=8787
SQLITE_PATH=./data/restrooms.sqlite

# Optional production-style database. If set to a postgres:// URL, the API and
# importer will use PostGIS instead of local SQLite.
# DATABASE_URL=postgres://restrooms:restrooms@localhost:5432/restrooms
